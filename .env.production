# App info
NUXT_PUBLIC_APP_NAME=Mohamed Essam
NUXT_PUBLIC_APP_ENV=production
# Replace with your live domain before deploy (no trailing slash).
# CI overrides this via repository variables NUXT_PUBLIC_SITE_URL_DEV / _PROD.
NUXT_PUBLIC_SITE_URL=https://example.com
