# Inspira UI components

Copy Inspira UI components here (shadcn-style). They are auto-registered as
global components via the `~/components` glob in nuxt.config.ts and their
utility classes are scanned by the `@source` directive in app/assets/css/main.css.

Add components on demand per phase (e.g. the hero background effect in Phase 1).
